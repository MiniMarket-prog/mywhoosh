// Add this to your existing types file or create it if it doesn't exist

export interface Goal {
    id: string
    name: string
    target_amount: number
    current_amount: number
    start_date: string
    end_date: string
    category: string
    type: string
    created_at: string
  }
  
  